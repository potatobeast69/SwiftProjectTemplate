# Swift Code Review Template

Автоматическая проверка Swift проектов через GitHub Actions.

## Что это?

Git-шаблон для автоматической проверки качества кода студентов. При каждом push запускаются проверки:

1. **🔨 Компиляция** - проверка сборки проекта
2. **🎨 Стиль кода** - SwiftLint анализ
3. **🗑️ Мертвый код** - поиск неиспользуемых классов/методов (Periphery)
4. **💾 Утечки памяти** - статический анализ retain cycles

**Важно:** Проверки 2-4 запускаются только если компиляция успешна.

## Быстрый старт

### 1. Создание Template Repository

```bash
# В Settings репозитория отметьте "Template repository"
```

### 2. Настройка

Обновите URL репозитория с инструментами в `.github/workflows/code-review.yml`:

```yaml
git clone https://github.com/${{ github.repository_owner }}/mesimtesto.git tools
```

Замените на URL вашего репозитория с Code Review Tools.

### 3. Использование студентами

1. Студент нажимает **"Use this template"** на GitHub
2. Клонирует созданный репозиторий
3. Добавляет свой Swift проект
4. Делает push → автоматические проверки запускаются

## Результаты

Все результаты доступны в:
- **GitHub Actions** → вкладка Actions в репозитории
- **Artifacts** → JSON отчеты для скачивания
- **Backend API** (опционально, требует настройки)

### Пример отчета

```json
{
  "repository": "student/project",
  "build_status": "success",
  "reports": {
    "style": { "errors": 5, "warnings": 10 },
    "dead_code": { "warnings": 3 },
    "memory": { "errors": 0, "warnings": 2 }
  }
}
```

## Интеграция с Backend

### Добавьте Secrets

В настройках репозитория студента:
- `BACKEND_URL` - URL вашего API endpoint
- `BACKEND_TOKEN` - токен авторизации

### Раскомментируйте код

В `.github/workflows/code-review.yml` (строка ~375):

```yaml
# TODO: Раскомментируйте когда backend будет готов
# if [ -n "${{ secrets.BACKEND_URL }}" ]; then
#   curl -X POST ${{ secrets.BACKEND_URL }} \
#     -H "Content-Type: application/json" \
#     -H "Authorization: Bearer ${{ secrets.BACKEND_TOKEN }}" \
#     -d @final-report.json
# fi
```

Уберите `#` для активации отправки.

## Настройка проверок

### SwiftLint

Отредактируйте `.swiftlint.yml`:

```yaml
disabled_rules:
  - line_length        # отключить проверку длины строк
  - trailing_whitespace

line_length:
  warning: 120
  error: 150
```

### Локальная установка инструментов

```bash
./scripts/setup.sh
```

Доступные команды после установки:
- `swift-style-check .`
- `swift-dead-code YourProject.xcodeproj`
- `swift-memory-check . --static-analysis`

## Требования

- GitHub Actions включены в репозитории
- macOS runner (для сборки Swift проектов)
- Поддерживаемые типы проектов:
  - Swift Package Manager (Package.swift)
  - Xcode проекты (.xcodeproj)
  - Xcode workspaces (.xcworkspace)

## Ограничения

- **Periphery** работает только с Xcode проектами (не SPM)
- **GitHub Actions минуты**:
  - Public репозитории: безлимит
  - Private: 2000 мин/месяц (бесплатно)
  - ~5-10 минут на один прогон

## Структура

```
.github/workflows/
  └── code-review.yml    # Workflow с 6 jobs
scripts/
  └── setup.sh           # Локальная установка инструментов
.swiftlint.yml          # Конфигурация SwiftLint
.gitignore              # Swift gitignore
README.md               # Этот файл
```

## FAQ

**Q: Проверки не запускаются?**
- Убедитесь что Actions включены в Settings
- Проверьте push в ветку `main` или `develop`

**Q: Слишком много ошибок SwiftLint?**
- Настройте `.swiftlint.yml` под свои требования
- Отключите строгие правила для начинающих

**Q: Как получить детальные логи?**
- GitHub Actions → выберите запуск → кликните на job

## Лицензия

Для образовательных целей.
